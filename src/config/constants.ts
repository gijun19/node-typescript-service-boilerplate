/**
 * Constants in here are values that do not change with the environment. Any values that change
 * with the environment should be in `config/index.ts`.
 */
export const constants = {
  app: {
    name: "headless-scraper",
    port: 3000,
  },
};
